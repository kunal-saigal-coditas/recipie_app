import 'package:flutter/material.dart';

class RecipieCategorySectionWidget extends StatelessWidget {
  const RecipieCategorySectionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
